const Discord = require('discord.js');

module.exports = async(message, args, client) => {
    //now paste the command code here!
    message.channel.send('This command works');
    //now save all the files and restart the bot.
}